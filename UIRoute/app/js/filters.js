var bookStoreFilters = angular.module('bookStoreFilters', []);

bookStoreDirectives.filter('bookStoreFilter_1', ['$scope',
    function($scope) {}
]);

bookStoreDirectives.filter('bookStoreFilter_2', ['$scope',
    function($scope) {}
]);